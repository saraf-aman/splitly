# Progress Log

Append one entry per completed step, most recent at the bottom. Keep entries short — bullet points, not paragraphs. This file (plus ROADMAP.md) is what a fresh chat session needs to pick up exactly where the last one left off, so it's worth being precise about deviations/decisions even if terse.

Entry template:

```
## [Step number] — [short title]  (YYYY-MM-DD)
- Built: <one or two lines on what was implemented>
- Files: <key files added/changed>
- Deviations/decisions: <anything that differs from PROJECT_PLAN.md or ROADMAP.md, or a judgment call made while building>
- Next session should know: <anything not obvious from the code itself>
```

---

**Pre-0.1 note (2026-07-14):** Added a "Git discipline" rule to `CLAUDE.md`: never run `git commit`/`git push` unless explicitly asked in the moment. **Correction:** an earlier version of this note said IntelliJ files (`splitly.iml`, `.idea/`) were removed — that was wrong/reverted. The user actively uses IntelliJ alongside this repo; those files must never be touched (crashes IntelliJ). See `CLAUDE.md` → "Environment notes".

## 0.2 — Firebase client SDK  (2026-07-14)
- Built: Firebase console setup (Auth w/ Google provider, Firestore, web app registration). Decided **not** to use Firebase Storage — receipt images are never persisted (see decision below) — so no Storage/Blaze plan needed. Created `src/lib/firebase.ts`: initializes the Firebase app from `NEXT_PUBLIC_FIREBASE_*` env vars, exports `auth` (`getAuth`) and `db` (Firestore via `initializeFirestore` with `persistentLocalCache()` for offline support). Guarded against Next.js dev re-initialization with a `getApps().length` check.
- Files: `.env.local` (gitignored, real Firebase config), `.env.example` (committed, placeholders), `src/lib/firebase.ts`, `package.json` (added `firebase` dependency).
- Deviations/decisions: Dropped Firebase Storage from the whole project entirely. Receipt images will be sent directly from the browser to the Claude parsing API route (Phase 2.2) and discarded — never written to any storage. This avoids the paid Blaze plan (Storage requires it as of Oct 2024) and matches the user's explicit **$0 running cost** goal. Updated `CLAUDE.md`, `docs/PROJECT_PLAN.md` (data model + Vercel timeout section), and `docs/ROADMAP.md` (steps 2.1/2.2) accordingly. Also flagged that Vercel Hobby's 10s function timeout (vs Pro's 60s) may constrain how the Claude parsing route is built in 2.2 — plan to fit within Hobby's limit rather than assume Pro.
- Next session should know: `lib/firebase.ts` is not imported/used anywhere yet — nothing calls `auth` or `db` until Phase 1.1 (Google Sign-In) wires up a real client component. `initializeFirestore`'s `persistentLocalCache` requires IndexedDB (browser-only), so anything importing this file must stay behind a `'use client'` boundary per the locked architecture decision.

## 0.3 — PWA setup (manifest + Serwist)  (2026-07-14)
- Built: `public/manifest.json` (standalone display, name/short_name "Splitly", theme color `#4f46e5`, 192×192 + 512×512 icons). `src/app/sw.ts` — Serwist service worker with a `NetworkOnly` rule for the 4 Firebase hostnames (firestore/firebase/fcmregistrations/identitytoolkit.googleapis.com) placed *before* `defaultCache`, so realtime traffic is never intercepted, per the locked architecture decision. `next.config.ts` wrapped with `withSerwistInit` (`swSrc: src/app/sw.ts`, `swDest: public/sw.js`, disabled in dev). `src/app/layout.tsx` updated with real `metadata` (manifest link, icons, `appleWebApp` config) and a `viewport` export for `themeColor`.
- Files: `public/manifest.json`, `public/icon-192.png`, `public/icon-512.png` (placeholder solid-color icons — swap for a real logo anytime), `src/app/sw.ts`, `next.config.ts`, `src/app/layout.tsx`, `package.json` (added `@serwist/next`/`serwist`; `dev`/`build` scripts now pass `--webpack`), `.gitignore` (added `/public/sw.js` — generated on every build, must not be committed).
- Deviations/decisions: **Next.js 16 defaults to Turbopack, but `@serwist/next` only supports webpack** (confirmed via a failed `next build` first). Pinned `dev`/`build` to `next dev/build --webpack` rather than switching to the experimental `@serwist/turbopack` package, to stay on the already-locked `@serwist/next` choice. Icons are placeholder solid-color PNGs (user opted for placeholder now, real logo later — no other code changes needed when swapped, just replace the two PNG files).
- Next session should know: Verified programmatically (not on a real device) — production build succeeds, `public/sw.js` generates with the Firebase exclusion baked in, and `curl` against a local dev server confirms `<link rel="manifest">`, `<meta name="theme-color">`, and `<link rel="apple-touch-icon">` all render correctly in the HTML `<head>`. **Not yet verified**: the actual "Add to Home Screen" prompt on real iOS Safari / Chrome Android — that needs the app reachable over HTTPS (or at least on the same network) from a phone, which means deploying (or at least a network-accessible dev server) first. Revisit once there's a deployed URL to test against.

## 0.1 — Next.js + TypeScript + Tailwind scaffold  (2026-07-14)
- Built: Scaffolded via `create-next-app` (App Router, TypeScript, Tailwind, ESLint, `src/` dir) into a temp dir then merged into the repo root to avoid clobbering existing files (`.env.local`, `CLAUDE.md`, `docs/`, `.idea/`). Added `src/components/`, `src/lib/`, `src/types/` per the planned folder structure. Set up Husky + lint-staged (`prepare` script auto-activates hooks on `npm install`; pre-commit runs `eslint --fix` + `tsc --noEmit` on staged `.ts`/`.tsx`). Verified `npm run dev` serves 200 on localhost:3000, and verified the pre-commit hook actually fires (tested with a real commit, then reverted it — should not have committed without asking, corrected immediately).
- Files: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`, `src/app/*`, `.husky/pre-commit`, `.gitignore` (merged/rewritten — see deviation below).
- Deviations/decisions: **`next lint` does not exist in Next 16** (this project's version) — the generated `package.json` uses plain `eslint` with a flat `eslint.config.mjs` (via `eslint-config-next`). Adjusted lint-staged and the pre-commit hook to call `eslint --fix` instead of `next lint --fix`, functionally equivalent. Also: `create-next-app` refused to scaffold directly into the repo root (non-empty dir) and its own generated `.gitignore` would have blanket-ignored `.env*` (which would've hidden `.env.example` from git, undesired) — rewrote `.gitignore` by hand to merge Next.js ignores with the existing IntelliJ/Eclipse/etc. ignores and keep `.env*.local`/`.env` ignored but `.env.example` tracked.
- Next session should know: Nothing is committed yet — all scaffold files are sitting as unstaged/untracked changes for the user to review and commit themselves. Node modules were installed locally (`npm install`) but are gitignored as usual. A moderate `npm audit` advisory exists in Next's bundled PostCSS (transitive); fixing it would downgrade Next significantly, so left as-is.

## 1.1 — Google Sign-In via Firebase Auth  (2026-07-14)
- Built: `src/lib/auth-context.tsx` — client-side `AuthProvider` + `useAuth()` hook wrapping Firebase's `onAuthStateChanged`, exposing `{ user, loading }`. `src/components/AuthGate.tsx` — client component that redirects logged-out users to `/login` and logged-in users away from `/login`, shown at the root layout so every route is covered without per-page boilerplate. `src/app/login/page.tsx` — Google sign-in button via `signInWithPopup` + `GoogleAuthProvider`. Wired `AuthProvider`/`AuthGate` into `src/app/layout.tsx`. Replaced the `create-next-app` boilerplate on `src/app/page.tsx` with a minimal signed-in view (shows display name/email + sign-out button) — this is a placeholder home page, not final UI.
- Files: `src/lib/auth-context.tsx`, `src/components/AuthGate.tsx`, `src/app/login/page.tsx`, `src/app/layout.tsx`, `src/app/page.tsx`.
- Deviations/decisions: Used `signInWithPopup` (not redirect) — simpler for a desktop-first dev flow; revisit if popup blockers cause issues on mobile Safari/PWA standalone mode later. Redirect logic lives in one shared `AuthGate` client component wrapping `{children}` in the root layout rather than per-page checks, per the `'use client'` boundary strategy in `PROJECT_PLAN.md` §11 (layout itself stays a server component; only `AuthGate`/`AuthProvider` are client).
- Next session should know: `tsc --noEmit` and `eslint` are clean, and both `/` and `/login` serve 200 with no server errors. **Not verified in a real browser** — the Claude-in-Chrome extension wasn't connected this session, so the actual Google OAuth popup → redirect → sign-out round trip has not been click-tested. Do that manually (or reconnect the extension) before trusting this is fully working. No Firestore household/member doc is created on sign-in yet — that's Phase 1.2/1.3.

## 1.2 — Firestore data model + security rules  (2026-07-14)
- Built: `src/types/firestore.ts` — TS interfaces (`Household`, `Member`, `Bill`, `BillItem`, `SharedCharge`) mirroring `PROJECT_PLAN.md` §6. `firestore.rules` — rules for `households`, `households/{id}/members`, `bills`, and their `items`/`sharedCharges` subcollections. `firebase.json` pointing at the rules file (no prior Firebase project config existed in-repo).
- Files: `src/types/firestore.ts`, `firestore.rules`, `firebase.json`, `docs/PROJECT_PLAN.md` (data model — added `createdBy` field).
- Deviations/decisions: Added a `createdBy: userId` field to the `households` doc (not in the original §6 sketch) to solve the bootstrapping problem — a brand-new household has no member docs yet, so the admin-only member-write rule can't yet apply to its creator. Rule: a household's `create` is allowed by any signed-in user naming themselves `createdBy`; the matching member doc can then be self-created as `role: 'admin'` only by that same `createdBy` user, one time. All other member writes require an existing admin. Bill/item/sharedCharge access is gated by membership in the bill's `householdId` (looked up via `get()` for the subcollections since they don't carry `householdId` directly).
- Next session should know: Rules are **not deployed** — no `firebase-tools` installed locally and no `.firebaserc`/project link exists yet. Deploy via `firebase deploy --only firestore:rules` (needs `firebase login` + `firebase use <project-id>` first) or paste `firestore.rules` into the Firebase console Rules tab before Phase 1.3 needs real household writes to succeed. Rules were written but not tested against the emulator — worth a quick emulator/console test once 1.3 (actual household creation flow) exists to exercise them end-to-end.

**Update (2026-07-14):** User installed `firebase-tools`, ran `firebase login` + `firebase use --add` (linked to project `splitly-e8c08`, `.firebaserc` now committed), and deployed the rules above. Verified live via `firebase deploy --only firestore:rules` reporting "already up to date." Rules deployment is a repeatable one-command step (`firebase deploy --only firestore:rules`) whenever `firestore.rules` changes going forward — login/project-link is one-time and already done.

## 1.3 — Household creation/join flow  (2026-07-14)
- Built: `src/lib/household.ts` — `createHousehold(user, name)` (3 sequential writes: household doc, creator's own admin member doc, `users/{uid}` reverse-index doc), `joinHousehold(user, householdId)` (self-add as `guest` + reverse-index write), plus two hooks: `useUserHousehold()` (subscribes to `users/{uid}` to find the caller's household) and `useHousehold(householdId)` (subscribes to the household doc itself). `src/components/HouseholdGate.tsx` — sits inside `AuthGate` in the root layout; redirects users with no household to `/onboarding`, and away from `/onboarding` once they have one. `src/app/onboarding/page.tsx` — toggle between "create household" (name only) and "join household" (paste a household ID as the invite code). Home page (`src/app/page.tsx`) now shows the household name and its ID as a copyable "Invite ID" once loaded.
- Files: `src/lib/household.ts`, `src/components/HouseholdGate.tsx`, `src/app/onboarding/page.tsx`, `src/app/page.tsx`, `src/app/layout.tsx` (wired `HouseholdGate`), `src/types/firestore.ts` (added `UserDoc`), `firestore.rules` (added `users/{userId}` collection + a self-join-as-guest branch on the members create rule), `docs/PROJECT_PLAN.md` (data model — added `users/{userId}`).
- Deviations/decisions: **Invite mechanism is just the household's raw Firestore document ID**, pasted by the joiner — no separate invite-token/link collection. Reasonable for a 3-4 person private household (per §2 scale); revisit only if this ever needs to be a shareable link rather than a copy-pasted ID. **`createHousehold` uses 3 sequential `await`s, not a `writeBatch`** — the member-doc bootstrap rule does `get()` on the household doc, and within one batched/transactional commit Firestore rules evaluate `get()`/`exists()` against *pre-commit* state, so a batch would see the household as not-yet-existing and reject the write. Sequential writes avoid this; a partial failure (e.g. network drop between writes 1 and 2) can leave an orphan household with no member — acceptable risk at this scale, not handled with rollback/retry logic. Self-join always grants `role: "guest"` (never `admin`) and requires the household to already `exists()` — an admin can promote a guest later in Phase 1.4's management screen; rules already forbid a non-admin from writing any role but their own initial `guest` self-join.
- Next session should know: Rules were redeployed after this session's changes (`firebase deploy --only firestore:rules` — confirmed a real upload, not a no-op). **Not yet click-tested in a real browser** — build/lint/typecheck are clean and `npm run build` succeeds, but the actual create → see home screen → sign out → sign back in → join-with-that-ID round trip hasn't been exercised against the live Firebase project. Do that before trusting the flow, ideally with two different Google accounts to verify the join path and that a second joiner correctly lands as `guest` not `admin`. Phase 1.4 (admin member-management screen) is next.

## 1.4 — Admin-only household management screen  (2026-07-14)
- Built: `src/lib/household.ts` — added `useMembers(householdId)` (realtime, ordered by `addedAt`), `updateMemberRole(householdId, memberId, role)`, `removeMember(householdId, memberId)`. `src/app/household/page.tsx` — lists all members with a role `<select>` (admin/guest) and a "Remove" button per row; shows an "Only admins can manage the household" message and a link home for non-admins instead of rendering the table. Home page (`src/app/page.tsx`) now shows a "Manage household" link, only rendered for the signed-in user's own admin role.
- Files: `src/lib/household.ts`, `src/app/household/page.tsx`, `src/app/page.tsx`.
- Deviations/decisions: **No Firestore rules changes needed** — `firestore.rules` already restricts member `update`/`delete` to `isHouseholdAdmin()` (from Phase 1.2), so a guest hitting this screen (or calling the SDK directly, bypassing the UI) gets a real permission-denied, not just a hidden button. Added a client-side-only guard preventing the *last remaining admin* from demoting or removing themselves (disables their own row's controls) — this isn't enforced in rules, just a UX safeguard against an admin accidentally leaving the household with zero admins (which would permanently lock out all future member management, since every member write requires an existing admin).
- Next session should know: Not yet click-tested in a real browser with two accounts (one admin, one guest) — build/lint/typecheck are clean and `npm run build` succeeds. Should verify: guest cannot see the "Manage household" link and gets bounced with the "only admins" message if they navigate to `/household` directly; role-change and remove actually propagate live to other open clients. Phase 2.1 (bill upload UI) is next.

**Update (2026-07-14): Creator/Admin/Guest hierarchy.** Reworked roles from a flat Admin/Guest split into a 3-tier hierarchy: **Creator** (the household's original creator, a permanent super-admin) **> Admin > Guest**. This is a locked decision meant to generalize to other sensitive actions in later phases, not just member management — see `CLAUDE.md` → "Roles & permissions" and `PROJECT_PLAN.md` §3.
- Built: No new `role` enum value — the creator is identified via `households/{id}.createdBy` (already existed), their `Member.role` stays `"admin"`. `firestore.rules` — added `isHouseholdCreator(householdId)`; member `update`/`delete` rules now: nobody (including the creator) can touch the creator's own member doc; the creator can update/remove any other member; a regular (non-creator) admin can only promote a guest→admin or remove a guest, never touch another admin. Redeployed via `firebase deploy --only firestore:rules` (confirmed real upload). `src/app/household/page.tsx` — rows now show "Creator" (uneditable, "Permanent") for the creator, editable controls for rows the current user is allowed to act on per the same matrix, and "Admin-only" (no controls) otherwise. Removed the earlier "last remaining admin can't demote/remove self" client-side guard — now moot, since the creator (who is structurally guaranteed to always exist and always be an admin) can never be demoted or removed by the new rule, so a household can never end up with zero admins.
- Files: `firestore.rules`, `src/app/household/page.tsx`, `docs/PROJECT_PLAN.md` (§3 Roles table), `CLAUDE.md` (Roles & permissions quick reference).
- Deviations/decisions: Confirmed with user — regular admins **can** remove guests (only admin-on-admin actions are creator-gated), and the creator is **permanent** for now (no self-demotion/removal, no ownership transfer feature).
- Next session should know: Same manual-testing gap as above, now also covering the 3-tier matrix: verify a non-creator admin's row for another admin shows the plain "Admin" label with no controls, and that a non-creator admin attempting a direct SDK call to change another admin's role gets permission-denied (rules enforce it, not just the UI).

**Update (2026-07-14): `/household` row copy polish.** No rules/behavior changes — confirmed self-demotion was already blocked by the same rule/UI logic from the hierarchy update above (a non-creator admin's own row was already non-editable, since editing requires either being the creator or the target being a guest). This pass only reworked the row's copy/layout in `src/app/household/page.tsx`:
- Removed the small role label that sat under each member's name — redundant once the right-hand side always shows the role one way or another.
- Right-hand side is now the single source of truth for role display: an editable row shows the `<select>` (options capitalized "Admin"/"Guest"); a non-editable row shows a plain capitalized label instead — `"Owner"` for the creator, `"Admin"` for any admin row the current viewer can't act on (covers both "another admin, viewed by a non-creator admin" and "your own admin row").
- Went through a couple of interim wordings first (creator tag "Creator" → "Owner"; placeholder text "Permanent"/"Can't change your own role"/"Admin-only" → blank cells → finally the single capitalized label above) — settled on the simplest version per user preference, no leftover dead text from the interim states.
- Files: `src/app/household/page.tsx` only. No `firestore.rules` change, so no redeploy needed this round.
- Next session should know: Still not click-tested live. Visual check worth doing once testable: creator's row reads "Owner" on the right with no controls; your own admin row reads "Admin" with no controls; another admin's row (viewed by a non-creator admin) also reads "Admin" with no controls; a guest's row always shows the editable dropdown + Remove button for any admin viewer.

**Update (2026-07-15): Fix — removed member's already-open tab kept apparent access.** User caught: after an admin removes a guest, that guest's browser tab (already open before the removal) kept showing household data instead of being kicked out, since nothing was watching for "did I just lose membership." The underlying Firestore rules were never actually breached (any read/write from the removed user was denied server-side the instant their member doc was deleted), but the client didn't react to that denial — realtime listeners (`useHousehold`, `useMembers`) just went silent on permission-denied and kept rendering whatever was last cached, and `users/{uid}`'s `householdId` pointer (which `HouseholdGate` uses to decide whether to show onboarding) was never cleared, so the stale tab never routed away.
- Built: `src/lib/household.ts` — `useHousehold`/`useMembers` now pass an error callback to `onSnapshot` that resets to `null`/`[]` on permission-denied instead of leaving stale data. Added `useMembershipStatus(householdId, uid)` — a dedicated listener on the current user's own `households/{id}/members/{uid}` doc; resolves `isMember: false` the instant that doc is deleted or becomes unreadable. Added `clearRemovedHouseholdPointer(user)` — deletes the user's own `users/{uid}` doc (the one write a removed user is still allowed to make, since `users/{userId}` rules only ever permit `request.auth.uid == userId`).
- `src/components/HouseholdGate.tsx` now calls `useMembershipStatus` alongside `useUserHousehold`: when it detects `householdId` is set but the membership doc is gone (`wasRemoved`), it shows a "You've been removed from this household" screen immediately (instead of rendering `children`) and self-clears the `users/{uid}` pointer, which cascades into `useUserHousehold` reporting no household and routing to `/onboarding` on its own.
- Files: `src/lib/household.ts`, `src/components/HouseholdGate.tsx`.
- Deviations/decisions: No `firestore.rules` changes — this was a client-side reactivity gap, not a rules gap; real data access was already correctly denied. Considered letting an admin directly clear the removed user's `users/{uid}` doc as part of the remove action, but the `users/{userId}` collection intentionally only allows self-writes (so one user's client can never touch another's top-level doc, in this household's data model or any future one); self-clearing on detection avoids loosening that rule.
- Next session should know: Not yet click-tested live with two tabs (one as the guest, one as the admin doing the removal) — should verify the guest's already-open tab flips to the "removed" screen within a second or two of the admin clicking Remove, without needing a manual reload. Phase 2.1 (bill upload UI) is next.

**Update (2026-07-15): Fix — stale `isMember` state broke rejoin (and even fresh household creation for a previously-removed test account).** User caught this immediately: after being removed then rejoining, the join looked like it worked from the admin's side (member doc really was recreated) but the joining user got kicked straight back out to onboarding — and the *same* symptom hit a fresh household creation with a test account that had previously been removed from another household earlier in the session.
- Root cause: `useMembershipStatus`'s `state` was only ever updated inside the Firestore `onSnapshot` callbacks — nothing reset it when `householdId`/`uid` changed. So after a removal left `state = { isMember: false }` and the user later rejoined (or created a new household), `HouseholdGate` briefly re-read that *stale* `isMember: false` the instant `householdId` pointed at the new/rejoined household, before the newly (re)subscribed listener had gotten its first real answer — and immediately treated it as another removal, self-clearing the just-written `users/{uid}` pointer it had only just gotten.
- Fix: `useMembershipStatus` now tracks which `householdId:uid` pair its `state` belongs to, and resets to `{ loading: true, isMember: true }` synchronously during render (React's supported "adjust state in response to a changed input" pattern) whenever that pair changes — not inside the effect, which would trip the `react-hooks/set-state-in-effect` lint rule for causing an extra cascading render. This closes the gap: `HouseholdGate` now correctly waits (`loading: true`) rather than briefly believing an already-resolved-elsewhere `isMember: false`.
- Files: `src/lib/household.ts` only.
- Next session should know: Please re-test the full remove → rejoin round trip live (same household), and also a from-scratch "create a new household" with an account that was previously removed from a different household in the same browser session — both were the exact repro paths reported broken. Phase 2.1 (bill upload UI) is next.

## 1.5 — Household deletion (creator-only)  (2026-07-15)
- Built: `deleteHousehold(householdId, creatorUid)` in `src/lib/household.ts` — cascades: queries top-level `bills` where `householdId == this household`, deletes each bill's `items`/`sharedCharges` subcollections then the bill doc; deletes every non-creator member doc; deletes the creator's own member doc; deletes the household doc last. `firestore.rules` — household `delete` split from `update` and restricted to `isHouseholdCreator(householdId)` (was any admin); members `delete` rule gained a narrow bypass letting the creator delete *their own* member doc (previously blocked unconditionally) specifically so this cascade can complete — no standalone "leave household" button uses this path. Redeployed via `firebase deploy --only firestore:rules` (confirmed real upload). `src/app/household/page.tsx` — creator-only "Danger zone" section: type the household's exact name into a text field to enable a "Delete household forever" button (no native `confirm()` — deliberately avoided since it can hang the PWA's event loop and doesn't fit the app's own UI).
- Files: `firestore.rules`, `src/lib/household.ts`, `src/app/household/page.tsx`.
- Deviations/decisions: **No new client-side notification needed for affected members** — every other open tab (guests, other admins, even the creator's own tab mid-deletion) already detects "my member doc is gone" via the `useMembershipStatus`/`HouseholdGate` machinery built in the two updates above, so deletion just needed to actually delete the docs; the existing removal-detection flow handles routing everyone to onboarding automatically. **Accepted risk**: the creator-self-delete rule bypass is scoped to `memberId == request.auth.uid && isHouseholdCreator`, with no rule-level guarantee it's only invoked as the *last* step of a full teardown — if that bypass were ever called standalone (bug or manual Firestore console action), the household would end up "headless" (no member matches `createdBy`, so no creator-gated action — including a future deletion — could ever run again), though bill-level access for remaining members would be unaffected. Deemed acceptable for a small trusted household, consistent with other accepted edge-case risks already documented for this project (e.g. orphan-household risk noted in Phase 1.3). Bill/item/sharedCharge cascade logic was written even though Phase 2 (bill upload) doesn't exist yet — the `bills` collection and its rules already exist, so this is currently a no-op path, not new scope.
- Deferred (raised by user, not built): **Multi-household membership** (a user belonging to more than one household). Recommended against bolting it on now — `users/{uid}.householdId` is a single-value pointer read by nearly every screen (`useUserHousehold`, `HouseholdGate`, `useHousehold`), so supporting multiple households is a foundational data-model change (e.g. a `users/{uid}/households/{id}` subcollection plus a "currently active household" concept and a switcher UI) best planned as its own `ROADMAP.md`/`PROJECT_PLAN.md` phase once core bill-splitting (Phases 2-4) works, not retrofitted mid-stream. Not yet added to `ROADMAP.md` — revisit if/when the user wants to scope it.
- Next session should know: Not yet click-tested live. Should verify: as the creator, typing the wrong name keeps the button disabled; after a successful delete, the creator's own tab and any other open member's tab both land on `/onboarding` within a couple seconds; a non-creator admin does not see the danger zone at all. Phase 2.1 (bill upload UI) is still next for feature work.

## 2.1 — Bill upload UI  (2026-07-15)
- Built: `src/app/bills/new/page.tsx` — a single file input (`accept="image/*"`, no `capture` attribute so iOS/Android show their native "Take Photo or choose from Library" chooser, covering both camera capture and file picker in one control) with an image preview, and an "Upload & parse" button. `src/lib/bills.ts` — `parseBillImage(image)` POSTs the file as `FormData` to `/api/parse-bill`; `createBill(user, householdId, parsed)` writes a new `bills/{id}` doc with `status: "pending_review"`. `src/app/api/parse-bill/route.ts` — **stub route**, not real parsing yet: validates an image was sent and returns a placeholder `{ restaurantOrStoreName: null, billDate: <now> }`. On successful parse, the page creates the bill doc and redirects home. Added an "Upload a bill" button on the home page, visible to any household member (not admin-gated).
- Files: `src/app/bills/new/page.tsx`, `src/lib/bills.ts`, `src/app/api/parse-bill/route.ts`, `src/app/page.tsx`.
- Deviations/decisions: **`/api/parse-bill` is intentionally a stub** — no Claude call yet, no image size/duration handling. Phase 2.2 replaces the stub body with the real Claude vision call and expands the response to include items/tax/tip/total; the client contract (`ParsedBillMeta` shape, `FormData` with an `image` field) is deliberately minimal so 2.2 can extend it without reshaping this page. No `firestore.rules` changes needed — `bills` create was already scoped to `isHouseholdMember(request.resource.data.householdId)` from Phase 1.2.
- Next session should know: Not yet click-tested live (build/typecheck/lint clean; `next build` succeeds with `/bills/new` and `/api/parse-bill` both compiling). Should verify on a real device: the file input actually offers a camera option on iOS/Android, and a successful upload creates a `pending_review` bill doc visible in the Firebase console. Phase 2.2 (real Claude API parsing) is next.

## 2.2 — Real receipt parsing (Google Gemini, not Claude)  (2026-07-15)
- **Locked decision changed:** receipt parsing now uses the **Google Gemini API (`gemini-2.5-flash`, free tier)** instead of the Claude API originally specified in `CLAUDE.md`/`PROJECT_PLAN.md`. Reason: the user does not want to run any paid, metered API on their own key when other household members' uploads would draw against it — Gemini's free tier (no card on file, generous daily request quota) is the only genuinely $0 option among vision-capable LLMs; Claude/OpenAI vision are both pay-per-token with no meaningful free tier. Updated `CLAUDE.md` (Tech stack → Receipt parsing) and `PROJECT_PLAN.md` (§2 flow, §11 Vercel timeout note) to reflect this — Claude is still used for parsing decisions/Claude Code itself, just not for this feature. Accepted risk, noted in `CLAUDE.md`: Google could tighten/remove the free tier later, or the quota could be hit under unusually heavy household use — revisit if that happens.
- Built: `src/app/api/parse-bill/route.ts` — replaced the Phase 2.1 stub with a real call via the `@google/genai` SDK, using a `responseSchema` (structured JSON output) to force the shape: `restaurantOrStoreName`, `billDate`, `items[]` (`name`, `price` in cents, `lowConfidence`), `tax`/`tip`/`serviceCharge`/`total` (cents, nullable). `maxDuration = 10` set explicitly to keep the route within Vercel Hobby's cap. `src/lib/bills.ts` — added client-side `downscaleImage()` (canvas-based, resizes to max 1600px + re-encodes as JPEG q0.8 before upload) to keep both the upload and Gemini's image-token cost small; `parseBillImage`/`createBill` updated for the fuller response shape. `src/types/firestore.ts` — added `ParsedReceiptItem`/`ParsedReceipt` types and a required `parsedResult: ParsedReceipt` field on `Bill` (stores the AI's raw guess; Phase 2.3's review screen edits a working copy of this, Phase 2.4's confirm is what writes the real `items`/`sharedCharges` subcollections — `parsedResult` itself is never treated as source of truth). `.env.example` — added `GEMINI_API_KEY` with a note on where to generate one for free.
- Files: `src/app/api/parse-bill/route.ts`, `src/lib/bills.ts`, `src/types/firestore.ts`, `.env.example`, `CLAUDE.md`, `docs/PROJECT_PLAN.md`.
- Deviations/decisions: No `firestore.rules` changes — `parsedResult` is just another field on a doc already covered by the existing `bills` create/read rules. Image downscaling was done client-side (canvas), not server-side, to avoid adding an image-processing dependency and to cut upload bandwidth too, not just Gemini's token cost.
- Next session should know: **`GEMINI_API_KEY` is not yet set in `.env.local` or Vercel** — the user needs to generate a free key at aistudio.google.com/apikey and add it themselves before this can be click-tested end-to-end (build succeeds regardless; the SDK only warns at build time when the key is absent, it doesn't fail the build). Once a key is added, verify: a real receipt photo produces sensible items/prices/tax/tip in the Firestore `parsedResult` field, and that low-quality/blurry line items actually come back `lowConfidence: true`. Phase 2.3 (review/edit screen) is next — it will be the first UI to actually display `parsedResult`, since nothing renders it today.

**Update (2026-07-15): Model fix — `gemini-2.5-flash` rejected for this key.** Live-tested `/api/parse-bill` after the user added their real `GEMINI_API_KEY`. The route returned 502; server logs showed Gemini rejecting the model with `404 ... "This model models/gemini-2.5-flash is no longer available to new users"` even though it still appears in `models.list()`. Listed available models directly against the user's key and confirmed `gemini-flash-latest` (an alias Google points at their current recommended flash model, rather than a specific dated version) works — switched `src/app/api/parse-bill/route.ts` to use it, to reduce the odds of this same breakage recurring when a specific dated model gets deprecated again.
- Also added `console.error` logging in the route's catch block (was a bare `catch {}` that swallowed the real error) — needed it to diagnose this, and it's generally useful for reading Vercel function logs later.
- Verified end-to-end with a real request: a blank test image correctly returns `{ items: [], tax: null, tip: null, serviceCharge: null, total: null, ... }` — confirms the key, the SDK call, and the response schema are all wired correctly. Not yet tested with a real receipt photo.
- Files: `src/app/api/parse-bill/route.ts`.
