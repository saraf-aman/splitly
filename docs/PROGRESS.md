# Progress Log

---

## Current state
_Update this block at the end of every session. This is the only section a new session needs to read — full history entries below are reference only._

- **Next step:** 7.1 — FCM setup (notification permission, device token, store on member doc)
- **Phase 6 progress:** complete — 6.1 through 6.4 all done (including post-6.4 polish: UX redesign, bug fixes, visual refinements)
- **Phases complete:** 0 (scaffold), 1 (auth+household), 2 (bill upload+parse), 3 (design system), 4 (bill review+confirm), 5 (realtime selection screen), 6 (final grid + calculations)
- **Dev server:** port 3001 (port 3000 is a different app on this machine)
- **Accent color:** Deep Teal `#2E6E6E` (swapped from amber after Phase 3.6); amber is used exclusively for owner-override UI (banner, checkboxes, Save button)
- **Gemini model:** `gemini-flash-lite-latest` (speed > accuracy to stay within Vercel Hobby 10s limit)
- **Money:** always integer cents everywhere — display formatting only in UI
- **Key rules:** `"use client"` on anything importing `firebase.ts`; `persistentLocalCache` = IndexedDB browser-only; `react-hooks/set-state-in-effect` lint rule enforced (no setState synchronously in effect bodies — derive from params instead)
- **shadcn Button override gotcha:** `variant="outline"` has `dark:bg-input/30` which competes with Tailwind bg overrides in className. Use `variant="default"` when you need a fully custom background color — `bg-primary` is the only bg class it sets and tailwind-merge cleanly overrides it.

---

_Entry template:_
```
## [Step] — [title]  (YYYY-MM-DD)
- Built / Files / Deviations / Next session should know
```

---

## 6.4 — Bill-owner override + post-6.4 polish  (2026-07-18)
- **Core feature:** Tightened `firestore.rules` items `update` rule — non-uploaders may only write to their own uid's selections key; uploader may write any member's key (via `.diff().affectedKeys()`). `updateItemSelection` in `bills.ts` gains optional `setBy` param (uploader passes own uid when overriding). `setBy` field added to `BillItem.selections` type.
- **Override UX:** Grid is purely informational — no cell interaction. Uploader sees a small "✏ Edit" button in each other member's column header → navigates to `/bills/[billId]/select?as=[memberId]`. Select page enters override mode when `?as` param is valid (viewer is uploader, target ≠ viewer): amber banner "Editing [Name]'s selections on their behalf", all reads/writes use `targetUid`, save button ("Save") returns to grid without writing `confirmedBy`.
- **Bug fix — confirmSelections not persisting:** `confirmSelections` was only writing `confirmedBy[uid]: true` but never the item selections, so the grid showed blanks for confirmed members. Fixed: now batch-writes every item's effective selection (`sel?.included ?? true`, `sel?.shares ?? 1`) before updating `confirmedBy`, so confirmed members' choices are always in Firestore.
- **Bug fix — override defaulting all items to checked:** Override mode was using the same `included = sel?.included ?? true` default as normal mode, forcing the uploader to uncheck and re-check every item to trigger a write. Fixed: `sel?.included ?? !overrideMemberId` — override starts unchecked (blank slate), normal mode stays defaulted-on.
- **Grid pill visual redesign:** Replaced plain checkmarks with filled rounded-pill badges: self-set items → `bg-green-500 text-white`; uploader-set items → `bg-amber-500 text-white`; unconfirmed with no selection → `bg-muted text-foreground`. Non-included cells show `—` (muted/30). These are clearly distinguishable from each other and from the dash.
- **Override select page styling:** Owner-set checkboxes → `accent-amber-700` (subtle dark amber, same depth as teal). Override banner → `bg-amber-100 border-amber-300 text-amber-900` (removed dark: variants that made it unreadable). Save button → `variant="default"` with `bg-amber-700 text-white hover:bg-amber-800` — using `variant="outline"` was broken because it injects `dark:bg-input/30` which competes with the Tailwind override; `variant="default"` only has `bg-primary` which tailwind-merge cleanly overrides.
- **Select page UX simplification:** Removed "Done: [names]" confirmed-by text. Removed "Selections confirmed ✓" secondary button state. Removed "View split grid" button. "Confirm my selections" → "Confirm"; clicking it now batch-writes all selections + `confirmedBy` then auto-navigates to grid. "Back to home" ghost button remains.
- **Shared charges in grid:** Added "SHARED CHARGES" tbody section between items and tfoot totals — one row per charge with lock icon, charge name, total amount, and per-member equal split (using `allocateEqually` from `splitCalc.ts`).
- **ESLint fix:** Added `public/sw.js` and `public/workbox-*.js` to `globalIgnores` in `eslint.config.mjs` — Serwist-generated service worker was being linted (1 error + many warnings).
- **Misc fixes:** Escaped apostrophe in JSX (`&apos;s`); fixed 3 IntelliJ boolean-simplification warnings (`overrideMemberId ? false : true` → `!overrideMemberId`, `confirmed ? true : false` → `confirmed`).
- Files: `firestore.rules`, `src/lib/bills.ts`, `src/lib/splitCalc.ts`, `src/app/bills/[billId]/grid/page.tsx`, `src/app/bills/[billId]/select/page.tsx`, `src/types/firestore.ts`, `eslint.config.mjs`
- Verified: Override page shows amber banner, amber checkboxes for owner-set items, solid amber-700 Save button. Grid shows green-500 pills (self), amber-500 pills (owner-set), — (excluded). Shared charges appear as locked rows with per-member equal splits. Confirm auto-navigates to grid and grid shows confirmed member's full item list. ✓

## 6.3 — Per-person totals row on grid  (2026-07-18)
- Built: Added `useSharedCharges` to grid page, computed `calculateSplit(items, charges, memberIds)` after all data is loaded, rendered a `<tfoot>` row with double top border. Confirmed members show exact total (`$X.XX`); unconfirmed show `~$X.XX`. Guarded `calculateSplit` behind `memberIds.length > 0` because `useMembers` returns `[]` before Firestore responds — without the guard the runtime invariant assertion threw immediately (caught in browser verification). Also caught in the same session: the runtime assertion in `calculateSplit` correctly fired on a real mismatch (195¢ of charges unallocated because members hadn't loaded yet), proving the cent-integrity guardrail works.
- Files: `src/app/bills/[billId]/grid/page.tsx`
- Deviations: none
- Verified: grid showed "Total" row — confirmed Aman $6.96 (exact), unconfirmed Aman ~$0.97 (tilde). $6.96 + $0.97 = $7.93 = 598¢ item + 195¢ charges. ✓

## 6.2 — Split calculation module  (2026-07-18)
- Built: Pure function `calculateSplit(items, charges, memberIds)` in `src/lib/splitCalc.ts`. Item costs divided proportionally by shares using largest-remainder method (floor each member's share, give extra cents to members with highest fractional loss). Shared charges split equally among all memberIds regardless of selections. Sum of all per-person totals is guaranteed to equal the sum of all item prices + all charge amounts. Installed Vitest (`npm install -D vitest`) + added `"test": "vitest run"` script + `vitest.config.ts`.
- Files: `src/lib/splitCalc.ts` (new), `src/lib/__tests__/splitCalc.test.ts` (new), `vitest.config.ts` (new), `package.json`
- Deviations: none
- Verified: 11/11 tests pass (`npx vitest run`), `tsc --noEmit` clean.

## 6.1 — Grid UI: items × members  (2026-07-18)
- Built: New page at `src/app/bills/[billId]/grid/page.tsx`. Status banner ("X of Y confirmed") with first-name chips — confirmed chips solid teal with a ✓, unconfirmed chips greyed with a clock icon. Horizontally scrollable table: sticky first column (item name + price stacked vertically), one column per member. Unconfirmed member columns get `bg-muted/30` tint + clock icon in the header. Cells: `✓` for included (shares=1), `✓ ×N` for shares>1 (self-authored cell uses primary color, others use foreground), `—` for not-included or no selection entry. No stepper — read-only display. Bottom bar: "Edit my selections" (navigates back to select), "Back to home". Added "View split grid" button to select page's bottom bar.
- Files: `src/app/bills/[billId]/grid/page.tsx` (new), `src/app/bills/[billId]/select/page.tsx`
- Deviations: `font-money` uses `text-right` internally so used `font-mono tabular-nums` for the price in the item column to keep it left-aligned below the item name.
- Verified: Navigated via "View split grid" from select page — grid loaded with real bill data, status banner showed "1 of 2 confirmed" with correct chip states, MAGGI NOODLES showed "✓ ×2" (had shares=2 written from prior testing), all other items showed "—", "Edit my selections" navigated back to select page correctly.

## 5.5 — Replace shares stepper with ⋮ kebab + bottom sheet  (2026-07-18)
- Built: Removed always-visible stepper and "Shares" column header. Each item row now has a vertical `⋮` (MoreVertical) icon at the right. Tapping it opens a bottom sheet with a large stepper (− / count / +), item name, and explanatory copy ("Covering someone outside the household? Increase this to pay for extra portions…"). When shares > 1 a `×N` badge appears on the icon. Icon is greyed and `disabled` when item is unchecked. Dynamic label below the counter: "just you" at 1, "you + N extra person/people" above that. Backdrop tap or ✕ closes the sheet.
- Files: `src/app/bills/[billId]/select/page.tsx`
- Deviations: none
- Verified: tapped ⋮ on MAGGI NOODLES, sheet opened, bumped to 2, label changed to "you + 1 extra person", ×2 badge appeared on the row icon in real time.

## 5.4 — Per-user "done" indicator on select screen  (2026-07-18)
- Built: `confirmedBy?: Record<string, boolean>` added to `Bill` type. `confirmSelections(billId, uid)` in `bills.ts` writes `confirmedBy.${uid}: true` via dot-notation `updateDoc`. Select page imports `useMembers(bill?.householdId ?? null)` and derives confirmed names to display above the button. Primary button switches from "Confirm my selections" (default teal) to "Selections confirmed ✓" (secondary style) on write; "Back to home" remains as ghost secondary below. No Firestore rule change needed — bill doc `update` was already open to any household member.
- Files: `src/types/firestore.ts`, `src/lib/bills.ts`, `src/app/bills/[billId]/select/page.tsx`
- Deviations: none
- Verified: clicked "Confirm my selections" in browser — button changed to "Selections confirmed ✓", "Done: Aman" appeared above it (realtime snap from `useBill`).

## 5.3 — Wire up Firestore writes on select screen  (2026-07-18)
- Built: `updateItemSelection(billId, itemId, uid, { included, shares })` in `src/lib/bills.ts` — writes `selections.${uid} = { included, shares, setBy: uid }` with dot-notation `updateDoc` so other users' entries are never overwritten. `handleToggle` and `handleShares` in `select/page.tsx` call it fire-and-forget; Firestore `onSnapshot` drives the displayed state (no local optimistic state needed — snapshot round-trip is fast enough). `setBy: uid` stored from first write onward for Phase 6.4 attribution.
- Files: `src/lib/bills.ts`, `src/app/bills/[billId]/select/page.tsx`
- Deviations: none
- Verified: checkbox click on bill `vxOkmvq2ClRgJn97wtRT` (MAGGI NOODLES) unchecked visually and `updateItemSelection` console log confirmed correct uid/billId/itemId — no permission errors. Debug log then removed.
- Note: `persistentLocalCache` in Firestore means reads are served from IndexedDB without server-side rule evaluation — so a bill that only exists locally in cache will show items but writes will fail with "Missing or insufficient permissions." Always use a real Firestore bill doc for write testing.

---

**Pre-0.1 note (2026-07-14):** Added a "Git discipline" rule to `CLAUDE.md`: never run `git commit`/`git push` unless explicitly asked in the moment. **Correction:** an earlier version of this note said IntelliJ files (`splitly.iml`, `.idea/`) were removed — that was wrong/reverted. The user actively uses IntelliJ alongside this repo; those files must never be touched (crashes IntelliJ). See `CLAUDE.md` → "Environment notes".

## 0.2 — Firebase client SDK  (2026-07-14)
- Built: Firebase console setup (Auth w/ Google provider, Firestore, web app registration). Decided **not** to use Firebase Storage — receipt images are never persisted (see decision below) — so no Storage/Blaze plan needed. Created `src/lib/firebase.ts`: initializes the Firebase app from `NEXT_PUBLIC_FIREBASE_*` env vars, exports `auth` (`getAuth`) and `db` (Firestore via `initializeFirestore` with `persistentLocalCache()` for offline support). Guarded against Next.js dev re-initialization with a `getApps().length` check.
- Files: `.env.local` (gitignored, real Firebase config), `.env.example` (committed, placeholders), `src/lib/firebase.ts`, `package.json` (added `firebase` dependency).
- Deviations/decisions: Dropped Firebase Storage from the whole project entirely. Receipt images will be sent directly from the browser to the Claude parsing API route (Phase 2.2) and discarded — never written to any storage. This avoids the paid Blaze plan (Storage requires it as of Oct 2024) and matches the user's explicit **$0 running cost** goal. Updated `CLAUDE.md`, `docs/PROJECT_PLAN.md` (data model + Vercel timeout section), and `docs/ROADMAP.md` (steps 2.1/2.2) accordingly. Also flagged that Vercel Hobby's 10s function timeout (vs Pro's 60s) may constrain how the Claude parsing route is built in 2.2 — plan to fit within Hobby's limit rather than assume Pro.
- Next session should know: `lib/firebase.ts` is not imported/used anywhere yet — nothing calls `auth` or `db` until Phase 1.1 (Google Sign-In) wires up a real client component. `initializeFirestore`'s `persistentLocalCache` requires IndexedDB (browser-only), so anything importing this file must stay behind a `'use client'` boundary per the locked architecture decision.

## 0.3 — PWA setup (manifest + Serwist)  (2026-07-14)
- Built: `public/manifest.json` (standalone display, name/short_name "Splitly", theme color `#4f46e5`, 192×192 + 512×512 icons). `src/app/sw.ts` — Serwist service worker with a `NetworkOnly` rule for the 4 Firebase hostnames (firestore/firebase/fcmregistrations/identitytoolkit.googleapis.com) placed *before* `defaultCache`, so realtime traffic is never intercepted, per the locked architecture decision. `next.config.ts` wrapped with `withSerwistInit` (`swSrc: src/app/sw.ts`, `swDest: public/sw.js`, disabled in dev). `src/app/layout.tsx` updated with real `metadata` (manifest link, icons, `appleWebApp` config) and a `viewport` export for `themeColor`.
- Files: `public/manifest.json`, `public/icon-192.png`, `public/icon-512.png` (placeholder solid-color icons — swap for a real logo anytime), `src/app/sw.ts`, `next.config.ts`, `src/app/layout.tsx`, `package.json` (added `@serwist/next`/`serwist`; `dev`/`build` scripts now pass `--webpack`), `.gitignore` (added `/public/sw.js` — generated on every build, must not be committed).
- Deviations/decisions: **Next.js 16 defaults to Turbopack, but `@serwist/next` only supports webpack** (confirmed via a failed `next build` first). Pinned `dev`/`build` to `next dev/build --webpack` rather than switching to the experimental `@serwist/turbopack` package, to stay on the already-locked `@serwist/next` choice. Icons are placeholder solid-color PNGs (user opted for placeholder now, real logo later — no other code changes needed when swapped, just replace the two PNG files).
- Next session should know: Verified programmatically (not on a real device) — production build succeeds, `public/sw.js` generates with the Firebase exclusion baked in, and `curl` against a local dev server confirms `<link rel="manifest">`, `<meta name="theme-color">`, and `<link rel="apple-touch-icon">` all render correctly in the HTML `<head>`. **Not yet verified**: the actual "Add to Home Screen" prompt on real iOS Safari / Chrome Android — that needs the app reachable over HTTPS (or at least on the same network) from a phone, which means deploying (or at least a network-accessible dev server) first. Revisit once there's a deployed URL to test against.

## 0.1 — Next.js + TypeScript + Tailwind scaffold  (2026-07-14)
- Built: Scaffolded via `create-next-app` (App Router, TypeScript, Tailwind, ESLint, `src/` dir) into a temp dir then merged into the repo root to avoid clobbering existing files (`.env.local`, `CLAUDE.md`, `docs/`, `.idea/`). Added `src/components/`, `src/lib/`, `src/types/` per the planned folder structure. Set up Husky + lint-staged (`prepare` script auto-activates hooks on `npm install`; pre-commit runs `eslint --fix` + `tsc --noEmit` on staged `.ts`/`.tsx`). Verified `npm run dev` serves 200 on localhost:3000, and verified the pre-commit hook actually fires (tested with a real commit, then reverted it — should not have committed without asking, corrected immediately).
- Files: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`, `src/app/*`, `.husky/pre-commit`, `.gitignore` (merged/rewritten — see deviation below).
- Deviations/decisions: **`next lint` does not exist in Next 16** (this project's version) — the generated `package.json` uses plain `eslint` with a flat `eslint.config.mjs` (via `eslint-config-next`). Adjusted lint-staged and the pre-commit hook to call `eslint --fix` instead of `next lint --fix`, functionally equivalent. Also: `create-next-app` refused to scaffold directly into the repo root (non-empty dir) and its own generated `.gitignore` would have blanket-ignored `.env*` (which would've hidden `.env.example` from git, undesired) — rewrote `.gitignore` by hand to merge Next.js ignores with the existing IntelliJ/Eclipse/etc. ignores and keep `.env*.local`/`.env` ignored but `.env.example` tracked.
- Next session should know: Nothing is committed yet — all scaffold files are sitting as unstaged/untracked changes for the user to review and commit themselves. Node modules were installed locally (`npm install`) but are gitignored as usual. A moderate `npm audit` advisory exists in Next's bundled PostCSS (transitive); fixing it would downgrade Next significantly, so left as-is.

## 1.1 — Google Sign-In via Firebase Auth  (2026-07-14)
- Built: `src/lib/auth-context.tsx` — client-side `AuthProvider` + `useAuth()` hook wrapping Firebase's `onAuthStateChanged`, exposing `{ user, loading }`. `src/components/AuthGate.tsx` — client component that redirects logged-out users to `/login` and logged-in users away from `/login`, shown at the root layout so every route is covered without per-page boilerplate. `src/app/login/page.tsx` — Google sign-in button via `signInWithPopup` + `GoogleAuthProvider`. Wired `AuthProvider`/`AuthGate` into `src/app/layout.tsx`. Replaced the `create-next-app` boilerplate on `src/app/page.tsx` with a minimal signed-in view (shows display name/email + sign-out button) — this is a placeholder home page, not final UI.
- Files: `src/lib/auth-context.tsx`, `src/components/AuthGate.tsx`, `src/app/login/page.tsx`, `src/app/layout.tsx`, `src/app/page.tsx`.
- Deviations/decisions: Used `signInWithPopup` (not redirect) — simpler for a desktop-first dev flow; revisit if popup blockers cause issues on mobile Safari/PWA standalone mode later. Redirect logic lives in one shared `AuthGate` client component wrapping `{children}` in the root layout rather than per-page checks, per the `'use client'` boundary strategy in `PROJECT_PLAN.md` §11 (layout itself stays a server component; only `AuthGate`/`AuthProvider` are client).
- Next session should know: `tsc --noEmit` and `eslint` are clean, and both `/` and `/login` serve 200 with no server errors. **Not verified in a real browser** — the Claude-in-Chrome extension wasn't connected this session, so the actual Google OAuth popup → redirect → sign-out round trip has not been click-tested. Do that manually (or reconnect the extension) before trusting this is fully working. No Firestore household/member doc is created on sign-in yet — that's Phase 1.2/1.3.

## 1.2 — Firestore data model + security rules  (2026-07-14)
- Built: `src/types/firestore.ts` — TS interfaces (`Household`, `Member`, `Bill`, `BillItem`, `SharedCharge`) mirroring `PROJECT_PLAN.md` §6. `firestore.rules` — rules for `households`, `households/{id}/members`, `bills`, and their `items`/`sharedCharges` subcollections. `firebase.json` pointing at the rules file (no prior Firebase project config existed in-repo).
- Files: `src/types/firestore.ts`, `firestore.rules`, `firebase.json`, `docs/PROJECT_PLAN.md` (data model — added `createdBy` field).
- Deviations/decisions: Added a `createdBy: userId` field to the `households` doc (not in the original §6 sketch) to solve the bootstrapping problem — a brand-new household has no member docs yet, so the admin-only member-write rule can't yet apply to its creator. Rule: a household's `create` is allowed by any signed-in user naming themselves `createdBy`; the matching member doc can then be self-created as `role: 'admin'` only by that same `createdBy` user, one time. All other member writes require an existing admin. Bill/item/sharedCharge access is gated by membership in the bill's `householdId` (looked up via `get()` for the subcollections since they don't carry `householdId` directly).
- Next session should know: Rules are **not deployed** — no `firebase-tools` installed locally and no `.firebaserc`/project link exists yet. Deploy via `firebase deploy --only firestore:rules` (needs `firebase login` + `firebase use <project-id>` first) or paste `firestore.rules` into the Firebase console Rules tab before Phase 1.3 needs real household writes to succeed. Rules were written but not tested against the emulator — worth a quick emulator/console test once 1.3 (actual household creation flow) exists to exercise them end-to-end.

**Update (2026-07-14):** User installed `firebase-tools`, ran `firebase login` + `firebase use --add` (linked to project `splitly-e8c08`, `.firebaserc` now committed), and deployed the rules above. Verified live via `firebase deploy --only firestore:rules` reporting "already up to date." Rules deployment is a repeatable one-command step (`firebase deploy --only firestore:rules`) whenever `firestore.rules` changes going forward — login/project-link is one-time and already done.

## 1.3 — Household creation/join flow  (2026-07-14)
- Built: `src/lib/household.ts` — `createHousehold(user, name)` (3 sequential writes: household doc, creator's own admin member doc, `users/{uid}` reverse-index doc), `joinHousehold(user, householdId)` (self-add as `guest` + reverse-index write), plus two hooks: `useUserHousehold()` (subscribes to `users/{uid}` to find the caller's household) and `useHousehold(householdId)` (subscribes to the household doc itself). `src/components/HouseholdGate.tsx` — sits inside `AuthGate` in the root layout; redirects users with no household to `/onboarding`, and away from `/onboarding` once they have one. `src/app/onboarding/page.tsx` — toggle between "create household" (name only) and "join household" (paste a household ID as the invite code). Home page (`src/app/page.tsx`) now shows the household name and its ID as a copyable "Invite ID" once loaded.
- Files: `src/lib/household.ts`, `src/components/HouseholdGate.tsx`, `src/app/onboarding/page.tsx`, `src/app/page.tsx`, `src/app/layout.tsx` (wired `HouseholdGate`), `src/types/firestore.ts` (added `UserDoc`), `firestore.rules` (added `users/{userId}` collection + a self-join-as-guest branch on the members create rule), `docs/PROJECT_PLAN.md` (data model — added `users/{userId}`).
- Deviations/decisions: **Invite mechanism is just the household's raw Firestore document ID**, pasted by the joiner — no separate invite-token/link collection. Reasonable for a 3-4 person private household (per §2 scale); revisit only if this ever needs to be a shareable link rather than a copy-pasted ID. **`createHousehold` uses 3 sequential `await`s, not a `writeBatch`** — the member-doc bootstrap rule does `get()` on the household doc, and within one batched/transactional commit Firestore rules evaluate `get()`/`exists()` against *pre-commit* state, so a batch would see the household as not-yet-existing and reject the write. Sequential writes avoid this; a partial failure (e.g. network drop between writes 1 and 2) can leave an orphan household with no member — acceptable risk at this scale, not handled with rollback/retry logic. Self-join always grants `role: "guest"` (never `admin`) and requires the household to already `exists()` — an admin can promote a guest later in Phase 1.4's management screen; rules already forbid a non-admin from writing any role but their own initial `guest` self-join.
- Next session should know: Rules were redeployed after this session's changes (`firebase deploy --only firestore:rules` — confirmed a real upload, not a no-op). **Not yet click-tested in a real browser** — build/lint/typecheck are clean and `npm run build` succeeds, but the actual create → see home screen → sign out → sign back in → join-with-that-ID round trip hasn't been exercised against the live Firebase project. Do that before trusting the flow, ideally with two different Google accounts to verify the join path and that a second joiner correctly lands as `guest` not `admin`. Phase 1.4 (admin member-management screen) is next.

## 1.4 — Admin-only household management screen  (2026-07-14)
- Built: `src/lib/household.ts` — added `useMembers(householdId)` (realtime, ordered by `addedAt`), `updateMemberRole(householdId, memberId, role)`, `removeMember(householdId, memberId)`. `src/app/household/page.tsx` — lists all members with a role `<select>` (admin/guest) and a "Remove" button per row; shows an "Only admins can manage the household" message and a link home for non-admins instead of rendering the table. Home page (`src/app/page.tsx`) now shows a "Manage household" link, only rendered for the signed-in user's own admin role.
- Files: `src/lib/household.ts`, `src/app/household/page.tsx`, `src/app/page.tsx`.
- Deviations/decisions: **No Firestore rules changes needed** — `firestore.rules` already restricts member `update`/`delete` to `isHouseholdAdmin()` (from Phase 1.2), so a guest hitting this screen (or calling the SDK directly, bypassing the UI) gets a real permission-denied, not just a hidden button. Added a client-side-only guard preventing the *last remaining admin* from demoting or removing themselves (disables their own row's controls) — this isn't enforced in rules, just a UX safeguard against an admin accidentally leaving the household with zero admins (which would permanently lock out all future member management, since every member write requires an existing admin).
- Next session should know: Not yet click-tested in a real browser with two accounts (one admin, one guest) — build/lint/typecheck are clean and `npm run build` succeeds. Should verify: guest cannot see the "Manage household" link and gets bounced with the "only admins" message if they navigate to `/household` directly; role-change and remove actually propagate live to other open clients. Phase 2.1 (bill upload UI) is next.

**Update (2026-07-14): Creator/Admin/Guest hierarchy.** Reworked roles from a flat Admin/Guest split into a 3-tier hierarchy: **Creator** (the household's original creator, a permanent super-admin) **> Admin > Guest**. This is a locked decision meant to generalize to other sensitive actions in later phases, not just member management — see `CLAUDE.md` → "Roles & permissions" and `PROJECT_PLAN.md` §3.
- Built: No new `role` enum value — the creator is identified via `households/{id}.createdBy` (already existed), their `Member.role` stays `"admin"`. `firestore.rules` — added `isHouseholdCreator(householdId)`; member `update`/`delete` rules now: nobody (including the creator) can touch the creator's own member doc; the creator can update/remove any other member; a regular (non-creator) admin can only promote a guest→admin or remove a guest, never touch another admin. Redeployed via `firebase deploy --only firestore:rules` (confirmed real upload). `src/app/household/page.tsx` — rows now show "Creator" (uneditable, "Permanent") for the creator, editable controls for rows the current user is allowed to act on per the same matrix, and "Admin-only" (no controls) otherwise. Removed the earlier "last remaining admin can't demote/remove self" client-side guard — now moot, since the creator (who is structurally guaranteed to always exist and always be an admin) can never be demoted or removed by the new rule, so a household can never end up with zero admins.
- Files: `firestore.rules`, `src/app/household/page.tsx`, `docs/PROJECT_PLAN.md` (§3 Roles table), `CLAUDE.md` (Roles & permissions quick reference).
- Deviations/decisions: Confirmed with user — regular admins **can** remove guests (only admin-on-admin actions are creator-gated), and the creator is **permanent** for now (no self-demotion/removal, no ownership transfer feature).
- Next session should know: Same manual-testing gap as above, now also covering the 3-tier matrix: verify a non-creator admin's row for another admin shows the plain "Admin" label with no controls, and that a non-creator admin attempting a direct SDK call to change another admin's role gets permission-denied (rules enforce it, not just the UI).

**Update (2026-07-14): `/household` row copy polish.** No rules/behavior changes — confirmed self-demotion was already blocked by the same rule/UI logic from the hierarchy update above (a non-creator admin's own row was already non-editable, since editing requires either being the creator or the target being a guest). This pass only reworked the row's copy/layout in `src/app/household/page.tsx`:
- Removed the small role label that sat under each member's name — redundant once the right-hand side always shows the role one way or another.
- Right-hand side is now the single source of truth for role display: an editable row shows the `<select>` (options capitalized "Admin"/"Guest"); a non-editable row shows a plain capitalized label instead — `"Owner"` for the creator, `"Admin"` for any admin row the current viewer can't act on (covers both "another admin, viewed by a non-creator admin" and "your own admin row").
- Went through a couple of interim wordings first (creator tag "Creator" → "Owner"; placeholder text "Permanent"/"Can't change your own role"/"Admin-only" → blank cells → finally the single capitalized label above) — settled on the simplest version per user preference, no leftover dead text from the interim states.
- Files: `src/app/household/page.tsx` only. No `firestore.rules` change, so no redeploy needed this round.
- Next session should know: Still not click-tested live. Visual check worth doing once testable: creator's row reads "Owner" on the right with no controls; your own admin row reads "Admin" with no controls; another admin's row (viewed by a non-creator admin) also reads "Admin" with no controls; a guest's row always shows the editable dropdown + Remove button for any admin viewer.

**Update (2026-07-15): Fix — removed member's already-open tab kept apparent access.** User caught: after an admin removes a guest, that guest's browser tab (already open before the removal) kept showing household data instead of being kicked out, since nothing was watching for "did I just lose membership." The underlying Firestore rules were never actually breached (any read/write from the removed user was denied server-side the instant their member doc was deleted), but the client didn't react to that denial — realtime listeners (`useHousehold`, `useMembers`) just went silent on permission-denied and kept rendering whatever was last cached, and `users/{uid}`'s `householdId` pointer (which `HouseholdGate` uses to decide whether to show onboarding) was never cleared, so the stale tab never routed away.
- Built: `src/lib/household.ts` — `useHousehold`/`useMembers` now pass an error callback to `onSnapshot` that resets to `null`/`[]` on permission-denied instead of leaving stale data. Added `useMembershipStatus(householdId, uid)` — a dedicated listener on the current user's own `households/{id}/members/{uid}` doc; resolves `isMember: false` the instant that doc is deleted or becomes unreadable. Added `clearRemovedHouseholdPointer(user)` — deletes the user's own `users/{uid}` doc (the one write a removed user is still allowed to make, since `users/{userId}` rules only ever permit `request.auth.uid == userId`).
- `src/components/HouseholdGate.tsx` now calls `useMembershipStatus` alongside `useUserHousehold`: when it detects `householdId` is set but the membership doc is gone (`wasRemoved`), it shows a "You've been removed from this household" screen immediately (instead of rendering `children`) and self-clears the `users/{uid}` pointer, which cascades into `useUserHousehold` reporting no household and routing to `/onboarding` on its own.
- Files: `src/lib/household.ts`, `src/components/HouseholdGate.tsx`.
- Deviations/decisions: No `firestore.rules` changes — this was a client-side reactivity gap, not a rules gap; real data access was already correctly denied. Considered letting an admin directly clear the removed user's `users/{uid}` doc as part of the remove action, but the `users/{userId}` collection intentionally only allows self-writes (so one user's client can never touch another's top-level doc, in this household's data model or any future one); self-clearing on detection avoids loosening that rule.
- Next session should know: Not yet click-tested live with two tabs (one as the guest, one as the admin doing the removal) — should verify the guest's already-open tab flips to the "removed" screen within a second or two of the admin clicking Remove, without needing a manual reload. Phase 2.1 (bill upload UI) is next.

**Update (2026-07-15): Fix — stale `isMember` state broke rejoin (and even fresh household creation for a previously-removed test account).** User caught this immediately: after being removed then rejoining, the join looked like it worked from the admin's side (member doc really was recreated) but the joining user got kicked straight back out to onboarding — and the *same* symptom hit a fresh household creation with a test account that had previously been removed from another household earlier in the session.
- Root cause: `useMembershipStatus`'s `state` was only ever updated inside the Firestore `onSnapshot` callbacks — nothing reset it when `householdId`/`uid` changed. So after a removal left `state = { isMember: false }` and the user later rejoined (or created a new household), `HouseholdGate` briefly re-read that *stale* `isMember: false` the instant `householdId` pointed at the new/rejoined household, before the newly (re)subscribed listener had gotten its first real answer — and immediately treated it as another removal, self-clearing the just-written `users/{uid}` pointer it had only just gotten.
- Fix: `useMembershipStatus` now tracks which `householdId:uid` pair its `state` belongs to, and resets to `{ loading: true, isMember: true }` synchronously during render (React's supported "adjust state in response to a changed input" pattern) whenever that pair changes — not inside the effect, which would trip the `react-hooks/set-state-in-effect` lint rule for causing an extra cascading render. This closes the gap: `HouseholdGate` now correctly waits (`loading: true`) rather than briefly believing an already-resolved-elsewhere `isMember: false`.
- Files: `src/lib/household.ts` only.
- Next session should know: Please re-test the full remove → rejoin round trip live (same household), and also a from-scratch "create a new household" with an account that was previously removed from a different household in the same browser session — both were the exact repro paths reported broken. Phase 2.1 (bill upload UI) is next.

## 1.5 — Household deletion (creator-only)  (2026-07-15)
- Built: `deleteHousehold(householdId, creatorUid)` in `src/lib/household.ts` — cascades: queries top-level `bills` where `householdId == this household`, deletes each bill's `items`/`sharedCharges` subcollections then the bill doc; deletes every non-creator member doc; deletes the creator's own member doc; deletes the household doc last. `firestore.rules` — household `delete` split from `update` and restricted to `isHouseholdCreator(householdId)` (was any admin); members `delete` rule gained a narrow bypass letting the creator delete *their own* member doc (previously blocked unconditionally) specifically so this cascade can complete — no standalone "leave household" button uses this path. Redeployed via `firebase deploy --only firestore:rules` (confirmed real upload). `src/app/household/page.tsx` — creator-only "Danger zone" section: type the household's exact name into a text field to enable a "Delete household forever" button (no native `confirm()` — deliberately avoided since it can hang the PWA's event loop and doesn't fit the app's own UI).
- Files: `firestore.rules`, `src/lib/household.ts`, `src/app/household/page.tsx`.
- Deviations/decisions: **No new client-side notification needed for affected members** — every other open tab (guests, other admins, even the creator's own tab mid-deletion) already detects "my member doc is gone" via the `useMembershipStatus`/`HouseholdGate` machinery built in the two updates above, so deletion just needed to actually delete the docs; the existing removal-detection flow handles routing everyone to onboarding automatically. **Accepted risk**: the creator-self-delete rule bypass is scoped to `memberId == request.auth.uid && isHouseholdCreator`, with no rule-level guarantee it's only invoked as the *last* step of a full teardown — if that bypass were ever called standalone (bug or manual Firestore console action), the household would end up "headless" (no member matches `createdBy`, so no creator-gated action — including a future deletion — could ever run again), though bill-level access for remaining members would be unaffected. Deemed acceptable for a small trusted household, consistent with other accepted edge-case risks already documented for this project (e.g. orphan-household risk noted in Phase 1.3). Bill/item/sharedCharge cascade logic was written even though Phase 2 (bill upload) doesn't exist yet — the `bills` collection and its rules already exist, so this is currently a no-op path, not new scope.
- Deferred (raised by user, not built): **Multi-household membership** (a user belonging to more than one household). Recommended against bolting it on now — `users/{uid}.householdId` is a single-value pointer read by nearly every screen (`useUserHousehold`, `HouseholdGate`, `useHousehold`), so supporting multiple households is a foundational data-model change (e.g. a `users/{uid}/households/{id}` subcollection plus a "currently active household" concept and a switcher UI) best planned as its own `ROADMAP.md`/`PROJECT_PLAN.md` phase once core bill-splitting (Phases 2-4) works, not retrofitted mid-stream. Not yet added to `ROADMAP.md` — revisit if/when the user wants to scope it.
- Next session should know: Not yet click-tested live. Should verify: as the creator, typing the wrong name keeps the button disabled; after a successful delete, the creator's own tab and any other open member's tab both land on `/onboarding` within a couple seconds; a non-creator admin does not see the danger zone at all. Phase 2.1 (bill upload UI) is still next for feature work.

## 2.1 — Bill upload UI  (2026-07-15)
- Built: `src/app/bills/new/page.tsx` — a single file input (`accept="image/*"`, no `capture` attribute so iOS/Android show their native "Take Photo or choose from Library" chooser, covering both camera capture and file picker in one control) with an image preview, and an "Upload & parse" button. `src/lib/bills.ts` — `parseBillImage(image)` POSTs the file as `FormData` to `/api/parse-bill`; `createBill(user, householdId, parsed)` writes a new `bills/{id}` doc with `status: "pending_review"`. `src/app/api/parse-bill/route.ts` — **stub route**, not real parsing yet: validates an image was sent and returns a placeholder `{ restaurantOrStoreName: null, billDate: <now> }`. On successful parse, the page creates the bill doc and redirects home. Added an "Upload a bill" button on the home page, visible to any household member (not admin-gated).
- Files: `src/app/bills/new/page.tsx`, `src/lib/bills.ts`, `src/app/api/parse-bill/route.ts`, `src/app/page.tsx`.
- Deviations/decisions: **`/api/parse-bill` is intentionally a stub** — no Claude call yet, no image size/duration handling. Phase 2.2 replaces the stub body with the real Claude vision call and expands the response to include items/tax/tip/total; the client contract (`ParsedBillMeta` shape, `FormData` with an `image` field) is deliberately minimal so 2.2 can extend it without reshaping this page. No `firestore.rules` changes needed — `bills` create was already scoped to `isHouseholdMember(request.resource.data.householdId)` from Phase 1.2.
- Next session should know: Not yet click-tested live (build/typecheck/lint clean; `next build` succeeds with `/bills/new` and `/api/parse-bill` both compiling). Should verify on a real device: the file input actually offers a camera option on iOS/Android, and a successful upload creates a `pending_review` bill doc visible in the Firebase console. Phase 2.2 (real Claude API parsing) is next.

## 2.2 — Real receipt parsing (Google Gemini, not Claude)  (2026-07-15)
- **Locked decision changed:** receipt parsing now uses the **Google Gemini API (`gemini-2.5-flash`, free tier)** instead of the Claude API originally specified in `CLAUDE.md`/`PROJECT_PLAN.md`. Reason: the user does not want to run any paid, metered API on their own key when other household members' uploads would draw against it — Gemini's free tier (no card on file, generous daily request quota) is the only genuinely $0 option among vision-capable LLMs; Claude/OpenAI vision are both pay-per-token with no meaningful free tier. Updated `CLAUDE.md` (Tech stack → Receipt parsing) and `PROJECT_PLAN.md` (§2 flow, §11 Vercel timeout note) to reflect this — Claude is still used for parsing decisions/Claude Code itself, just not for this feature. Accepted risk, noted in `CLAUDE.md`: Google could tighten/remove the free tier later, or the quota could be hit under unusually heavy household use — revisit if that happens.
- Built: `src/app/api/parse-bill/route.ts` — replaced the Phase 2.1 stub with a real call via the `@google/genai` SDK, using a `responseSchema` (structured JSON output) to force the shape: `restaurantOrStoreName`, `billDate`, `items[]` (`name`, `price` in cents, `lowConfidence`), `tax`/`tip`/`serviceCharge`/`total` (cents, nullable). `maxDuration = 10` set explicitly to keep the route within Vercel Hobby's cap. `src/lib/bills.ts` — added client-side `downscaleImage()` (canvas-based, resizes to max 1600px + re-encodes as JPEG q0.8 before upload) to keep both the upload and Gemini's image-token cost small; `parseBillImage`/`createBill` updated for the fuller response shape. `src/types/firestore.ts` — added `ParsedReceiptItem`/`ParsedReceipt` types and a required `parsedResult: ParsedReceipt` field on `Bill` (stores the AI's raw guess; Phase 2.3's review screen edits a working copy of this, Phase 2.4's confirm is what writes the real `items`/`sharedCharges` subcollections — `parsedResult` itself is never treated as source of truth). `.env.example` — added `GEMINI_API_KEY` with a note on where to generate one for free.
- Files: `src/app/api/parse-bill/route.ts`, `src/lib/bills.ts`, `src/types/firestore.ts`, `.env.example`, `CLAUDE.md`, `docs/PROJECT_PLAN.md`.
- Deviations/decisions: No `firestore.rules` changes — `parsedResult` is just another field on a doc already covered by the existing `bills` create/read rules. Image downscaling was done client-side (canvas), not server-side, to avoid adding an image-processing dependency and to cut upload bandwidth too, not just Gemini's token cost.
- Next session should know: **`GEMINI_API_KEY` is not yet set in `.env.local` or Vercel** — the user needs to generate a free key at aistudio.google.com/apikey and add it themselves before this can be click-tested end-to-end (build succeeds regardless; the SDK only warns at build time when the key is absent, it doesn't fail the build). Once a key is added, verify: a real receipt photo produces sensible items/prices/tax/tip in the Firestore `parsedResult` field, and that low-quality/blurry line items actually come back `lowConfidence: true`. Phase 2.3 (review/edit screen) is next — it will be the first UI to actually display `parsedResult`, since nothing renders it today.

**Update (2026-07-15): Model fix — `gemini-2.5-flash` rejected for this key.** Live-tested `/api/parse-bill` after the user added their real `GEMINI_API_KEY`. The route returned 502; server logs showed Gemini rejecting the model with `404 ... "This model models/gemini-2.5-flash is no longer available to new users"` even though it still appears in `models.list()`. Listed available models directly against the user's key and confirmed `gemini-flash-latest` (an alias Google points at their current recommended flash model, rather than a specific dated version) works — switched `src/app/api/parse-bill/route.ts` to use it, to reduce the odds of this same breakage recurring when a specific dated model gets deprecated again.
- Also added `console.error` logging in the route's catch block (was a bare `catch {}` that swallowed the real error) — needed it to diagnose this, and it's generally useful for reading Vercel function logs later.
- Verified end-to-end with a real request: a blank test image correctly returns `{ items: [], tax: null, tip: null, serviceCharge: null, total: null, ... }` — confirms the key, the SDK call, and the response schema are all wired correctly. Not yet tested with a real receipt photo.
- Files: `src/app/api/parse-bill/route.ts`.

**Update (2026-07-15): Fix — real timeout risk against Vercel Hobby's 10s cap.** User asked whether parsing could time out. Tested empirically (not just estimated): a representative-size image (1600px long edge, matching the client's `downscaleImage` cap) took **13.9s** end-to-end against the live key using `gemini-flash-latest` — over the 10s Hobby limit, meaning real receipt uploads could fail in production. Compared against `gemini-flash-lite-latest`: same test image completed in under 1s (**~1.3s** through the full route). Switched `src/app/api/parse-bill/route.ts` to `gemini-flash-lite-latest`.
- Deviation/decision: accepted the accuracy/speed tradeoff of the "lite" model variant — it's more likely to misread an unusual/blurry receipt than the full `flash` model, but that's exactly what the existing `lowConfidence` per-item flag exists to catch and route to human review (Phase 2.3), and reliably finishing inside Vercel's hard timeout matters more than marginal accuracy gains that would just fail outright over budget.
- Next session should know: worth re-verifying timing once Phase 2.3 sends a real, denser multi-item receipt photo (not a synthetic blank/noise test image) — a receipt with lots of text may take longer for the model to reason over even at the same pixel dimensions, though the 10x+ margin observed here should absorb that.

**Update (2026-07-15): Live-verified end-to-end with a real receipt (Vercel prod + phone).** Root-caused the user's earlier "could not parse, try again" report on their phone: `GEMINI_API_KEY` had been added in Vercel with the wrong scope (Preview only, plus an accidental typo'd duplicate `GEMINI_API_KE`) — Production (what the phone/PWA actually hits) had no key at all. Fixed by the user directly in the Vercel dashboard (added to Production, removed the typo'd duplicate, redeployed).
- Also resolved a follow-up question that looked like a bug but wasn't: user saw `$11.87` stored as `1187` in Firestore and asked if that's expected — confirmed yes, per the locked integer-cents convention (`CLAUDE.md`/`PROJECT_PLAN.md` §6). Verified directly by running a real Instacart receipt photo through the live parser: every item, tax ($0.05→5), tip ($1.00→100), service fee ($1.64→164), and total ($59.41→5941) came back exactly correct. No dollar-formatted display exists yet anywhere in the app — that's Phase 2.3/4's job, not a gap in 2.2.
- **Phase 2.2 is now considered fully done and live-verified**, not just build-clean. Phase 2.3 (review/edit screen) is next — first UI to actually render `parsedResult` for a human to check/correct before confirming.

## 3.1 — Design system foundation (shadcn/ui)  (2026-07-15)
- Built: ran `npx shadcn@latest init -d` (base-nova style, `@base-ui/react` primitives, `lucide-react` icons — shadcn's current default, not the older Radix-direct setup `PROJECT_PLAN.md` §12 was written against; functionally equivalent, same accessible-primitives rationale) then `npx shadcn@latest add input label card`, giving `src/components/ui/{button,input,label,card}.tsx` untouched from the generated versions — all theming done via CSS variables, not per-component edits. Rewrote `src/app/globals.css`: replaced the default shadcn `:root`/`.dark` token pairs with a **single light-mode-only `:root` block** using the exact hex values from `PROJECT_PLAN.md` §12 (`background #FAFAF9`, `card/surface #FFFFFF`, `foreground/ink #1A1A1F`, `primary/accent #C6893A`, `secondary` & `accent` both mapped to `accent-soft #FAF0E0` for chip/hover backgrounds, `muted-foreground #6B7280`, `border/input #E5E7EB`, `ring` = accent) plus a new `--success: #16A34A` token (not part of shadcn's default palette, added since §12 calls it out as a separate semantic color) — no `.dark` block, no `@media (prefers-color-scheme)`. Fixed `@theme inline`'s font mapping (`--font-sans`/`--font-heading`) to point at the project's actual `--font-geist-sans` variable from `layout.tsx`, which the generated file had left dangling. Added a small `@layer base` block with a type scale (`.text-display`/`.text-heading`/`.text-body`/`.text-caption`) and a `.font-money` utility (Geist Mono + `tabular-nums` + right-aligned) implementing the "money in mono" signature treatment from §12 — defined now as reusable tokens, not yet applied to any real page content (that's Phase 4+ as items/totals get built).
- Files: `components.json` (new), `src/components/ui/button.tsx`, `input.tsx`, `label.tsx`, `card.tsx` (new, shadcn-generated), `src/lib/utils.ts` (new, shadcn's `cn()` helper), `src/app/globals.css`, `package.json`/`package-lock.json` (added `@base-ui/react`, `class-variance-authority`, `clsx`, `lucide-react`, `shadcn`, `tailwind-merge`, `tw-animate-css`).
- Deviations/decisions: left the generated components' own `dark:` utility classes in place (e.g. in `button.tsx`/`input.tsx`) rather than hand-stripping them — they're inert since no `.dark` class is ever applied anywhere and nothing toggles it, so stripping them would just be unnecessary drift from shadcn's upstream generated code for no visual effect. Did not touch `src/app/layout.tsx`'s `themeColor` (`#4f46e5`, still indigo) or the icon PNGs — those are explicitly Phase 3.6's job per the roadmap, not 3.1's.
- Verified: `tsc --noEmit`, `next lint` (clean aside from a pre-existing, gitignored `public/sw.js` build artifact that was already flagging warnings before this session — unrelated to this change), and `next build` all pass; confirmed `/login` still renders (200) under `next dev` with no visible change, since this step intentionally adds no new UI, only tokens/primitives.
- Next session should know: Phase 3.2 (shared app shell — bottom tab nav) is next; it's the first step that will actually surface the new tokens/primitives visually.

## 3.2 — Shared app shell (bottom tab nav)  (2026-07-15)
- Built: `src/components/AppShell.tsx` — client component rendering a persistent, mobile-first bottom tab bar (Home `/`, Bills `/bills/new`, Household `/household`) using `lucide-react` icons already installed by 3.1 (`House`, `Receipt`, `Users` — the `Receipt` icon already has the jagged/torn-edge + ledger-line look §12 asked for, so no custom SVG was needed). Active tab match is prefix-based (`/bills*`, `/household*`) except Home which requires an exact `/` match; active tab renders `text-primary` (amber) with a slightly bolder stroke, inactive tabs `text-muted-foreground`. Nav is `fixed inset-x-0 bottom-0` with `bg-card`, a `border-t`, and `pb-[env(safe-area-inset-bottom)]` for iOS home-bar clearance; wrapped content gets `pb-20` so the fixed bar never overlaps page content. `SHELL_LESS_PATHS = ["/login", "/onboarding"]` makes the component render bare children (no nav) on those two routes, per spec. Wired into `src/app/layout.tsx` as the innermost wrapper: `AuthGate > HouseholdGate > AppShell > {children}` — sits inside both gates so it only ever mounts once a user is resolved (loading/redirect states render their own centered UI with no shell, unchanged from before).
- Files: `src/components/AppShell.tsx` (new), `src/app/layout.tsx`.
- Deviations/decisions: Did not touch any existing page content (`page.tsx`'s inline "Manage household" link, `dark:` classes, etc.) even though §12 frames the nav shell as "replacing" those ad hoc links — that restyle is explicitly 3.4/3.5's scope, not 3.2's; right now the nav bar and the old inline links coexist (harmless duplication) until those steps land.
- Verified live in the browser against the user's own running `npm run dev` instance (not just build-clean): nav renders and highlights the correct tab on `/`, `/bills/new`, and `/household`; tapping a tab navigates and updates the active state instantly. Did not click-test `/onboarding`/`/login` shell-less behavior live (would need a fresh, not-yet-onboarded account to reach `/onboarding` naturally) — verified by code inspection only (pathname check is unconditional and matches exactly).
- Next session should know: Phase 3.3 (restyle `/login` and `/onboarding` against the new primitives) is next.

## 3.3 — Restyle auth & onboarding screens  (2026-07-15)
- Built: `src/app/login/page.tsx` — replaced the old `zinc`/`indigo`/`dark:` Tailwind classes with design-system tokens (`bg-background`, `text-foreground`, `text-muted-foreground`, `text-destructive`) and the new type-scale utilities (`.text-display` for the "Splitly" heading, `.text-body` for the subhead); sign-in CTA is now the shadcn `Button` primitive (amber `primary` by default) sized up to `h-12 px-8 text-base` for a mobile-friendly touch target. `src/app/onboarding/page.tsx` — same token/primitive swap, plus: the create/join segmented toggle now uses `bg-secondary`/`bg-card` (amber-tinted pill background, white active-segment chip) instead of `zinc-200`/`white`+`dark:` pairs; the two forms are wrapped in a `Card`/`CardContent` (first real use of the `surface` token — a white card floating on the warm-white page background); each input now has a real `Label` (`Household name` / `Household ID`) above it instead of relying on placeholder text alone, using the `Label`/`Input` primitives from 3.1 for the first time; inputs bumped to `h-11 text-base` to match the button's touch-target sizing.
- Files: `src/app/login/page.tsx`, `src/app/onboarding/page.tsx`.
- Deviations/decisions: Bumped `Button`/`Input` heights above shadcn's dense default (`h-8`/`h-11` via `className` override) on both screens' primary actions — the base-nova shadcn style defaults to compact, dashboard-density controls, which read as too small for a mobile-first PWA's primary CTAs; sizing decision, not a token change, so it's applied per-instance via `className` rather than editing the primitive files. Added visible `Label`s to onboarding inputs (there were none before, only placeholders) since `Label` was already an unused primitive from 3.1 and this was the natural place to start using it — small a11y improvement bundled into the restyle, not scope creep.
- Verified: `tsc --noEmit`, `next build` both clean. **`/login` live-verified by the user directly** (screenshot confirmed: paper-white background, ink heading, amber CTA, muted caption — matches §12 exactly). **`/onboarding` not live-verified** — the only available test account already has a household, so navigating there just bounces back to `/` via `HouseholdGate`; would need a fresh, not-yet-onboarded Google account to see it rendered. Reviewed by code inspection only.
- Next session should know: Phase 3.4 (restyle home page + `/household`) is next. If a second/fresh Google test account ever becomes available, worth circling back to eyeball `/onboarding` live — low risk since it reuses the exact same tokens/primitives already confirmed working on `/login`.

## 3.4 — Restyle home page + household management screen  (2026-07-15)
- Added two new shadcn primitives needed for this step: `npx shadcn@latest add select badge` → `src/components/ui/select.tsx`, `src/components/ui/badge.tsx`. `src/app/page.tsx` — swapped `zinc`/`indigo`/`dark:` classes for tokens (`bg-background`, `text-body`/`text-heading`, `text-primary`); household name/invite ID now live in a `Card`; "Upload a bill" is the `Button` primitive (`size="lg"`, amber) rendered as a `Link` via base-ui's `render` prop (`nativeButton={false}` required — Base UI's `Button` warns/errors if `render`ing a non-`<button>` element without it, since the resulting DOM element loses native button semantics); "Manage household" gained a `Settings` icon; "Sign out" is now the `Button` `outline` variant instead of a hand-rolled bordered button. `src/app/household/page.tsx` — member rows are now `Card`s; role dropdown uses the new `Select` primitive; "Remove" uses `Button variant="destructive"` (soft red, shadcn default); non-editable roles ("Owner"/"Admin") render as an `outline` `Badge` instead of plain text; Danger zone is a `Card` with a `ring-destructive/30` accent (fixed after initial live-check — `Card`'s outline is a `ring-1 ring-foreground/10` utility, not `border-*`, so an earlier `border-destructive/30` attempt set a border-color with no border-width and rendered invisibly; user caught this from the screenshot, `ring-destructive/30` correctly overrides via `cn()`/`twMerge`), real `Label`+`Input` for the confirm-name field, and the final "Delete household forever" CTA kept as a solid red button via `bg-destructive text-white` (not the default soft `destructive` button variant) since this one specific action should read as more alarming than an inline row "Remove" — deliberately inconsistent with the Button primitive's default destructive styling for that reason.
- Files: `src/app/page.tsx`, `src/app/household/page.tsx`, `src/components/ui/select.tsx` (new), `src/components/ui/badge.tsx` (new).
- Deviations/decisions: No `--destructive-foreground` token exists in `globals.css` (shadcn's default `destructive` button/badge variants use a soft `bg-destructive/10 text-destructive` look that doesn't need one) — for the one place a solid destructive CTA was wanted (final delete button), used explicit `text-white` rather than inventing a new unused token for a single instance.
- Verified: `tsc --noEmit` and `eslint` both clean. **Live-verified both screens** in the browser against the user's running `npm run dev` instance: home page renders the amber card/CTA/link correctly; `/household` renders member cards, the `Select` dropdown opens with correct amber-active-item styling, typing the exact household name into the confirm field correctly enables the solid-red delete button (did not actually click it). Noted and ruled out as unrelated: a "1 issue" Next.js dev overlay on `/` is a pre-existing hydration mismatch from a Grammarly browser extension injecting `data-gr-ext-installed`/`data-new-gr-c-s-check-loaded` attributes onto `<body>` before hydration — not caused by this change, not present in a real (non-extension) browser/production PWA context.
- Next session should know: Phase 3.5 (restyle `/bills/new`) is next.

## 3.5 — Restyle bill upload screen (`/bills/new`)  (2026-07-15)
- Built: `src/app/bills/new/page.tsx` — swapped `zinc`/`indigo`/`dark:` classes for tokens (`bg-background`, `text-heading`, `text-muted-foreground`, `text-destructive`); the dropzone is now a `Card`/`CardContent` (with `p-0` overrides so the inner `label`'s own `px-6 py-10` dashed border owns all the padding, matching the flush-card pattern) wrapping the existing file-input label, with a `Camera` (`lucide-react`) icon in `text-primary` replacing the old text-only placeholder; "Upload & parse" is now the `Button` primitive (`size="lg"`, `h-12`, amber, matches the sizing convention set on `/` and `/login`/`/onboarding`) instead of a hand-rolled `indigo-600` button.
- Files: `src/app/bills/new/page.tsx`.
- Deviations/decisions: None — straightforward token/primitive swap, no new primitives needed (`Camera` icon already available via `lucide-react`, already a dependency since 3.1).
- Verified: `tsc --noEmit` and `eslint` both clean. **Live-verified the empty state** in the browser against the user's running `npm run dev` instance: amber camera icon, dashed-border card, muted caption text, and a disabled (light-amber, low-opacity) "Upload & parse" button all render correctly; bottom nav correctly highlights "Bills" as active. **Did not live-verify the file-selected state** (image preview + enabled button) — selecting a file triggers the OS-level native file picker, which browser automation can't drive; code inspection confirms `disabled={!file || submitting}` and the `previewUrl` conditional are unchanged from the pre-restyle logic, only the styling around them changed.
- Next session should know: Phase 3.6 (app icons + `theme_color`/`themeColor` amber swap) is next — the last step of Phase 3.

## 3.6 — App icons + theme color amber swap  (2026-07-15)
- Built: generated `public/icon-192.png`/`icon-512.png` programmatically (Python + Pillow, installed via `pip3 install pillow` for this session since no image tooling existed) — amber (`#C6893A`) rounded-square background (supersampled 4x then downscaled for smooth corners/edges) with a bold paper-white (`#FAFAF9`) "S" wordmark centered, matching `PROJECT_PLAN.md` §12's spec. Updated `theme_color` in `public/manifest.json` and `themeColor` in `src/app/layout.tsx`'s `viewport` export from indigo (`#4f46e5`) to amber (`#C6893A`).
- Files: `public/icon-192.png`, `public/icon-512.png`, `public/manifest.json`, `src/app/layout.tsx`.
- Deviations/decisions: No source vector/design asset existed for the "S" wordmark, so it was generated directly as a raster PNG via a one-off script rather than hand-drawn/exported from a design tool — script not committed (scratchpad only), only the resulting PNGs are checked in.
- Verified: `tsc --noEmit` clean; `next lint`'s one error is pre-existing/unrelated (gitignored `public/sw.js` build artifact, same issue noted in 3.1). **Live-verified** against the user's running `npm run dev` instance: `/icon-512.png` renders the amber rounded-square with the white "S" correctly; `/manifest.json` confirmed serving `"theme_color": "#C6893A"`; `/` page's rendered `<meta name="theme-color">` tag confirmed `#C6893A` via direct DOM read.
- **Phase 3 (UI design system & modernization) is now fully complete** — all steps 3.1–3.6 checked off in `ROADMAP.md`. Phase 4 (bill review & confirm) is next.

## 5.1 — Item list UI with realtime Firestore listener  (2026-07-18)
- Built: `useBill(billId)` + `useBillItems(billId)` hooks in `src/lib/bills.ts` — both use `onSnapshot` for realtime updates; `useBillItems` returns `(BillItem & { id: string })[]`. `src/app/bills/[billId]/select/page.tsx` — selection screen: heading with bill name, column header row (Price / Shares), one Card per item showing teal checkbox (`included ?? true`), item name, price in `font-money`, and a shares badge (`shares ?? 1`). Guards for bill-not-found and `pending_review` status. Sticky "Back to home" bottom bar (placeholder for 5.4's "confirm" button). Changed post-confirm redirect in `/bills/[billId]/review/page.tsx` from `/` to `/bills/${billId}/select`.
- Files: `src/lib/bills.ts`, `src/app/bills/[billId]/select/page.tsx` (new), `src/app/bills/[billId]/review/page.tsx`.
- Deviations/decisions: Checkboxes are controlled (`checked={included}`) with a noop `onChange` — correct shape for when 5.3 wires up writes (just replace the noop). Items are read-only display at this phase; all selections default to `true`/`1` since `selections` is `{}` on every item until 5.3 writes to it.
- Next session should know: **Live-verified** in browser against real Firestore data (PRIME FOOD MARKET bill, 9 items) — all items render with teal checkboxes checked, prices correctly formatted, shares badge showing 1. The dev server is running on port 3001 (not 3000 — another app is on 3000). Phase 5.2 (shared charges as locked rows) is next.

## 4.1 — Bill review/edit screen  (2026-07-17)
- Built: `/bills/[billId]/review/page.tsx` — uploader sees all parsed items in an editable list. Each row has an inline name `<Input>`, a dollar-formatted price `<Input>` (cents↔dollars conversion with `onBlur` normalization), and a delete button. Items flagged `lowConfidence: true` by Gemini get an amber `<AlertTriangle>` icon and an amber card border. "Add item" button appends a blank row. Shared charges (tax, tip, service charge) are shown as editable fields in a section below the items. "Confirm bill" button saves the edited working copy back to `parsedResult` on the bill doc (via new `updateBillParsedResult()`) and redirects to `/`. Non-uploader household members see a "Bill is being reviewed" placeholder.
- `src/app/bills/new/page.tsx` — changed post-upload redirect from `/` to `/bills/${billId}/review` so the uploader lands directly on the review screen.
- `src/lib/bills.ts` — added `getBill(billId)` (one-time `getDoc`) and `updateBillParsedResult(billId, parsedResult)` (`updateDoc`).
- Files: `src/app/bills/[billId]/review/page.tsx` (new), `src/app/bills/new/page.tsx`, `src/lib/bills.ts`.
- Deviations/decisions: Tax/tip/service charge fields included as editable (not read-only) on the review screen — not called out in the roadmap step description, but practically necessary since a misread tax/tip would corrupt the final split calculation, and the code is trivial.
- Next session should know: **Not live-verified** — couldn't automate Google OAuth to sign in and upload a real receipt; route exists and redirects to login correctly (no 404/compile error, `tsc` clean). User should verify end-to-end by uploading a receipt and confirming the redirect lands on the review screen with correct items. The "Confirm bill" button currently only saves `parsedResult` and goes home (status stays `pending_review`) — Phase 4.2 will extend it to write subcollections and set status to `open`.

## 4.2 — Confirm action  (2026-07-17)
- Built: `confirmBill(billId, items, charges)` in `src/lib/bills.ts` — uses a single `writeBatch` to atomically write all item docs to `bills/{id}/items`, all non-zero shared-charge docs to `bills/{id}/sharedCharges`, and flip `bills/{id}.status` to `"open"`. Stub notification is a `console.log("[splitly] bill confirmed…")` tagged for replacement in Phase 7. Items are written with `selections: {}` so Phase 5's realtime listener finds the correct shape immediately.
- Review page (`/bills/[billId]/review`): `handleConfirm` replaced — now calls `confirmBill` (not `updateBillParsedResult`); only shared charges with amount > 0 are written. Added a status guard before the uploader check: if `bill.status !== "pending_review"` the page shows "Bill already confirmed" instead of the edit UI, preventing re-submission.
- Files: `src/lib/bills.ts`, `src/app/bills/[billId]/review/page.tsx`.
- Deviations/decisions: Used `writeBatch` (not sequential `addDoc`s) so the three writes (items, charges, status update) are atomic — a partial failure can't leave items written but status still `pending_review`. Firestore batch write limit is 500 ops; a household receipt with hundreds of line items would exceed this, but 3-4 person grocery/restaurant bills are nowhere near that in practice.
- Next session should know: **Not live-verified** — requires signing in and uploading a real receipt end-to-end. After confirming, the Firestore console should show `bills/{id}` with `status: "open"`, an `items` subcollection with one doc per line item (each with `selections: {}`), and a `sharedCharges` subcollection with tax/tip/service docs where applicable. Phase 5 (realtime selection screen) is next.

## Post-3.6 — Accent color changed from amber to deep teal  (2026-07-15)
- User didn't like the amber accent after seeing it live ("not great"). Presented 6 candidate accents (Deep Pine, Slate Blue, Brick Clay, Deep Teal, Muted Plum, Charcoal Navy) as a Claude Artifact mockup, applied to the actual nav/card/badge/button layout with the existing paper-white background unchanged. User picked **Deep Teal `#2E6E6E`** (soft tint `#E3EEEE`).
- Built: swapped every amber hex in `src/app/globals.css` (`--primary`, `--secondary`, `--accent`, `--accent-soft`, `--ring`, and the `--sidebar-*` equivalents) to teal — all confined to the single `:root` token block from 3.1, no per-component edits needed. Updated `public/manifest.json` `theme_color` and `src/app/layout.tsx` `themeColor` from `#C6893A` to `#2E6E6E`. Regenerated `public/icon-192.png`/`icon-512.png` (same Pillow script from 3.6, just swapped the fill color) with the teal background. Updated `PROJECT_PLAN.md` §12's accent/token table and app icon description to match (was still describing amber).
- Files: `src/app/globals.css`, `public/manifest.json`, `src/app/layout.tsx`, `public/icon-192.png`, `public/icon-512.png`, `docs/PROJECT_PLAN.md`.
- Deviations/decisions: None beyond the color swap itself — confirms the token-based approach from 3.1 paid off exactly as intended (a full app-wide recolor took ~13 lines of CSS, no component touched).
- Verified: `tsc --noEmit` clean. **Live-verified** against the user's running `npm run dev` instance: home page (button, active "Home" tab, "Manage household" link) and `/household` (active "Household" tab) both render the new teal correctly; destructive/red elements untouched as expected.
